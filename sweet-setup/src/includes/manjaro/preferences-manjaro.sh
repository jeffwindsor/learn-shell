function postinstall.set_manjaro_preferences(){
  # ? inputs for git global name & username ?
  # ? personalized arch/manjaro stuff needs to be added
  # ?   tweaks - left buttons, theme, background
  # ?   vm settings - power savings?
  # ?   start to document them here
}